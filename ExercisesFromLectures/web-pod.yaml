apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
    name: web
spec:
  containers:
  - name: test-web-app
    image: ivaylokenov/simple-web-app:2.0
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 8080